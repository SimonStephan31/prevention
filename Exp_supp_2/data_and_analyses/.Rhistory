# read data
tdata <- read.table("Exp_supp2_data.txt", header=TRUE, sep="\t", na.strings="NA", dec=".", strip.white=TRUE)
# Demographics
#demogr_data <- tdata[!duplicated(tdata$sID), ]
mean(tdata$age)
sd(tdata$age)
min(tdata$age)
max(tdata$age)
table(tdata$gender)
mean(tdata$age)
min(tdata$age)
max(tdata$age)
table(tdata$gender)
rmarkdown::render_site()
